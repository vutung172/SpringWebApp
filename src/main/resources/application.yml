server:
    port: 8011
spring:
    application:
        name: webapp
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: Vtt$88842648864
        url: jdbc:mysql://localhost:3306/warehouse_manager
        username: root
    jpa:
        hibernate:
            ddl-auto: update
            naming:
                implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
                physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        properties:
            hibernate:
                format_sql: true
                show_sql: true
        show-sql: true
    servlet:
        multipart:
            enabled: true
            max-file-size: 50MB
            max-request-size: 50MB

#logging:
#  level:
#    root: info
#    com.ra.web.api: INFO
##    com.ra.api.controller: INFO
#  pattern:
#    console: ${LOG_PATTERN:%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n}
#  file:
#    name: public/logs/WebApp.log
#    path: public/logs/
#
JwtToken:
    key: "JWT_SECRET_KEY"